// Debounce delay (ms) for auto-persisting Primal Calculation edits — the Next
// Day Projection write-back and the Ending Stock carry-over save. Shared so
// both debounced saves stay in lockstep.
export const SAVE_DEBOUNCE_MS = 600;
